{
  "name": "freesail-dev",
  "version": "0.8.5",
  "private": true,
  "description": "Freesail - Agent-driven UI SDK",
  "scripts": {
    "clean": "npm run clean --workspaces --if-present",
    "sync": "node scripts/sync-versions.js",
    "build:core": "npm run build -w @freesail/logger && npm run build -w @freesail/core && npm run build -w @freesail/react",
    "build:cli": "npm run build:cli -w freesail",
    "build:sdk": "npm run build -w freesail && npm run build -w @freesail/gateway && npm run build -w @freesail/agent-runtime",
    "build:catalogs": "npm run build -w @freesail/standard-catalog && npm run build -w @freesail/chat-catalog && npm run build -w @freesail-community/weather-catalog",
    "build": "npm run sync && npm run build:core && npm run build:cli && npm run build:catalogs && npm run build:sdk",
    "clean:publish": "rm -rf publish && mkdir publish",
    "pack:all": "npm run clean:publish && npm run pack:packages",
    "pack:packages": "npm pack --workspaces --pack-destination=./publish"
  },
  "keywords": [
    "ai",
    "agent-driven ui",
    "generative ui",
    "a2ui",
    "agent"
  ],
  "author": "Thendral AI Ltd",
  "contributors": [
    "Shanmugam Sudalaimuthu"
  ],
  "license": "MIT",
  "workspaces": [
    "packages/@freesail/*",
    "packages/@freesail-community/*",
    "packages/freesail",
    "example/agent",
    "example/react-app"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/node": "^20.10.0",
    "glob": "^13.0.3",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@langchain/google-genai": "^2.1.20",
    "vite": "^8.0.7"
  }
}
